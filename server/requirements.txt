fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# headless build: the server has no display, and the GUI variant drags in system
# libraries (libGL, X11) that are not present in a slim container
opencv-python-headless==4.10.0.84
numpy==2.1.3
